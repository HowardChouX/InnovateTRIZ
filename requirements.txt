# TRIZ AI 创新助手 - 依赖包

## 核心框架
flet>=0.25.0
httpx>=0.25.0

## 数据存储和序列化
sqlalchemy>=2.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0

## AI集成
openai>=1.12.0,<2.0.0  # 用于DeepSeek API，锁定主版本避免breaking changes

## Excel数据读取（已弃用，数据已内置）
# xlrd>=2.0.0  # 数据已内置，无需外部读取

## 工具和工具
typing-extensions>=4.0.0
loguru>=0.7.0  # 日志记录

## 开发依赖 (可选)
pytest>=7.0.0
black>=23.0.0
mypy>=1.0.0
pytest-asyncio>=0.21.0

## 打包工具 (可选)
pyinstaller>=5.0.0
flet-cli>=0.25.0